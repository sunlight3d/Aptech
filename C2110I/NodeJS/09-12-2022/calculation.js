console.log('This is calculation')
const PI = 3.1416
function sum(x, y) {
    return x + y
}
const multiply = (x, y) =>  {
    return x * y
}
const substract = (x, y) => x - y
module.exports = {
    sum, 
    PI, 
    multiply, 
    substract
}
