
const INITIAL_STATE = {
    productName: '',
    year: ''
} 
export default function productReducer(state = INITIAL_STATE, action) {    
    switch (action.type) {      
      default:        
        debugger
        return state
    }
  }
